<Page x:Class="widget3.Views.Settings.CreateTilePages.SelectTypePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:widget3.Views.Settings.CreateTilePages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="SelectTypePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ComboBox Style="{DynamicResource ComboBoxStyle}" SelectedItem="{Binding SelectedType}" Margin="5" ItemsSource="{Binding TileTypes}"></ComboBox>
    </Grid>
</Page>
